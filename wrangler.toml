# Cloudflare Pages 部署配置
[build]
  command = "npm run build"
  destination = "dist"

[build.environment]
  NODE_VERSION = "20"

# 多语言重定向
[[redirects]]
  from = "/en/*"
  to = "/en/:splat"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/404"
  status = 404
