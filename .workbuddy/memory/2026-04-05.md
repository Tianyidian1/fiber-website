# 2026-04-05 工作日志

## AI 搜索引擎优化三步改造（08:45）

### 第一步：优化产品页结构（FAQ 模块）
- **文件**: `src/pages/products/pm-patch-cord.astro`
- 在产品列表页末尾新增「常见问题解答」板块
- 采用"问题+数据+解决方案"内容框架，6 个 FAQ 问答
- FAQ 主题覆盖：PM 跳线用途、消光比选择、插损影响、波长/接头类型、APC vs PC 区别、轴向识别
- 新增交互式折叠展开（`<details>` + CSS 动画）

### 第二步：Schema 结构化标记
- **文件**: `src/layouts/Layout.astro` + `src/pages/products/pm-patch-cord.astro`
- Layout 支持传入 `schema` prop，自动注入 `<script type="application/ld+json">`
- pm-patch-cord 页同时传入三种 Schema：
  - `Product`：产品名称、品牌、参数规格、供货状态
  - `FAQPage`：6 个问答结构化标记
  - `HowTo`：4 步选型指南（波长→消光比→接头→长度/护套）

### 第三步：海外客户案例
- **文件**: `src/pages/about.astro` + `src/pages/en/about.astro`
- 在"核心团队"和"CTA"板块之间插入「海外客户案例」板块
- 4 个场景案例（已虚构化处理）：
  | 国家 | 行业 | 核心价值点 |
  |------|------|------------|
  | 🇩🇪 德国 | 光纤激光研究 | 消光比 ER ≥ 30 dB，实验成功率 60%→98% |
  | 🇺🇸 美国 | 数据中心 | 批量 MPO-12，节省 30% 采购成本 |
  | 🇯🇵 日本 | 量子计算 | 超低插损 PM980 + 逐只测试报告 |
  | 🇬🇧 英国 | 电信 OEM | 专属产线，18 个月零退货 |
- 每个案例使用"挑战→方案→结果"三段式结构

### 构建结果
- ✅ 构建成功，25 页，2.61s
- 无新增 lint 错误

## GitHub Pages 部署准备（17:12）

- 编写并保存 `GitHub部署手册.md`（包含完整8步操作指南）
- 创建 `.github/workflows/deploy.yml`（GitHub Actions 自动部署工作流）
- 创建 `.gitignore`（排除 node_modules、dist、.env）
- 注意：用户使用 GitHub 默认域名时，需在 `astro.config.mjs` 中设置 `base: '/仓库名'`；使用自定义域名则只需设置 `site`

## CMS 后台配置 - 方案变更（18:03）

**变更**：从 GitHub Pages + GitHub OAuth 改为 Netlify + Git Gateway（方案 A）

**原因**：用户选择方案 A，Netlify 原生支持 Decap CMS，配置更简单

**已完成的配置修改**：
1. `public/admin/config.yml`：backend 改回 `git-gateway`，移除 GitHub 相关配置
2. `astro.config.mjs`：site 改为 `https://your-site-name.netlify.app`（占位，部署后更新）
3. 编写 `Netlify部署指南.md`（完整8步操作指南）

**后续操作**：
1. 注册 Netlify 账号（推荐用 GitHub 登录）
2. 连接 GitHub 仓库到 Netlify
3. 配置构建命令：`npm run build`，发布目录：`dist`
4. 启用 Netlify Identity + Git Gateway
5. 部署后访问 `/admin` 即可登录后台

## Git 环境配置与代码推送（00:25-00:54）

**问题**：系统未安装 Git 命令行工具，无法直接执行 git 命令

**解决方案**：
1. 用户已安装 GitHub Desktop（路径：`C:\Users\nikey\AppData\Local\GitHubDesktop\app-3.5.7`）
2. 配置 Git 环境变量，添加 GitHub Desktop 自带的 Git 路径
3. 配置 Git 用户信息：
   - user.name: Tianyidian1
   - user.email: wenyudemao@163.com

**执行的操作**：
1. 初始化本地 Git 仓库
2. 添加所有文件到暂存区
3. 提交代码："初始提交：光纤跳线网站"
4. 修改分支为 main
5. 修正远程仓库地址（原地址仓库名错误显示为 `-`）
6. 推送代码到 GitHub：https://github.com/Tianyidian1/fiber-website

**结果**：✅ 代码成功推送到 GitHub 仓库

**使用工具**：GitHub Desktop 自带的 Git（完整路径）
- 路径：`C:\Users\nikey\AppData\Local\GitHubDesktop\app-3.5.7\resources\app\git\cmd\git.exe`
